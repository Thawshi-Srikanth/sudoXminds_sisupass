{{define "subject"}}SisuPass - Verify Your Email Address{{end}}

{{define "plainBody"}}
Hello {{.name}},

Please verify your email address for your SisuPass account.

Click the link below to verify your email:
{{.verification_url}}

Your verification code: {{.verification_code}}

If you didn't request this verification, please ignore this email.

Best regards,
The SisuPass Team
{{end}}

{{define "htmlBody"}}
<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Verify Your Email - SisuPass</title>
    <style>
        body { font-family: Arial, sans-serif; line-height: 1.6; color: #333; background-color: #f4f4f4; margin: 0; padding: 20px; }
        .container { max-width: 600px; margin: 0 auto; background-color: white; padding: 30px; border-radius: 10px; box-shadow: 0 0 10px rgba(0,0,0,0.1); }
        .header { text-align: center; margin-bottom: 30px; }
        .logo { color: #2196F3; font-size: 28px; font-weight: bold; }
        .button { display: inline-block; background-color: #2196F3; color: white; padding: 15px 30px; text-decoration: none; border-radius: 5px; margin: 20px 0; font-weight: bold; }
        .button:hover { background-color: #1976D2; }
        .footer { margin-top: 30px; padding-top: 20px; border-top: 1px solid #eee; font-size: 12px; color: #666; text-align: center; }
        .code { background-color: #e3f2fd; padding: 15px; border-radius: 4px; font-family: monospace; font-size: 18px; text-align: center; letter-spacing: 2px; }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <div class="logo">📧 SisuPass</div>
            <h1 style="color: #2196F3; margin: 10px 0;">Verify Your Email</h1>
        </div>
        
        <p>Hello <strong>{{.name}}</strong>,</p>
        
        <p>Thank you for signing up with SisuPass! To complete your account setup, please verify your email address.</p>
        
        <div style="text-align: center;">
            <a href="{{.verification_url}}" class="button">Verify Email Address</a>
        </div>
        
        <p>Or enter this verification code in the app:</p>
        <div class="code">{{.verification_code}}</div>
        
        <p>Or copy and paste this link into your browser:</p>
        <p style="word-break: break-all; color: #666; background-color: #f8f9fa; padding: 10px; border-radius: 4px;">{{.verification_url}}</p>
        
        <p><strong>Why verify your email?</strong></p>
        <ul>
            <li>🔒 Secure your account</li>
            <li>📱 Receive important notifications</li>
            <li>🔄 Enable account recovery</li>
        </ul>
        
        <p>If you didn't create this account, please ignore this email.</p>
        
        <div class="footer">
            <p>Best regards,<br>The SisuPass Team</p>
            <p>This is an automated message. Please do not reply to this email.</p>
        </div>
    </div>
</body>
</html>
{{end}}